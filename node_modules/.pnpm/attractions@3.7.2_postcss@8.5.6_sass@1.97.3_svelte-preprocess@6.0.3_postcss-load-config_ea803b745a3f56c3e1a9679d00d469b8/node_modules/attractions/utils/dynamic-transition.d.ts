/**
 * @typedef {import('svelte/transition').TransitionConfig} TransitionConfig
 */
/**
 * Create a transition that allows specifying a transition programmatically
 *  or disable it altogether.
 * @template T
 * @param {Element} node
 * @param {{ transition: (node: Element; options: T) => TransitionConfig; options: T }} options
 * @returns {TransitionConfig}
 */
export default function dynamic<T>(node: Element, { transition, options }: {
    transition: (node: Element, options: T) => TransitionConfig;
    options: T;
}): TransitionConfig;
export type TransitionConfig = import('svelte/transition').TransitionConfig;
//# sourceMappingURL=dynamic-transition.d.ts.map